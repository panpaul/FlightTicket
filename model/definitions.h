/**
 * @file definitions.h
 * @brief definitions of the length used in this project
 * @author Paul
 * @version 0.0.2
 * @date 2020-5-7
 */

#ifndef _DEFINITIONS_H_
#define _DEFINITIONS_H_

/// The max length of the customer's name
#define CUSTOMER_NAME_MAX_SIZE 20*sizeof(char)
/// The max length of the customer's id
#define CUSTOMER_ID_MAX_SIZE 30*sizeof(char)

/// The max length of the flight's name
#define FLIGHT_NAME_MAX_SIZE 20*sizeof(char)
/// The max length of the departure's name
#define FLIGHT_DEPARTURE_MAX_SIZE 20*sizeof(char)
/// The max length of the destination's name
#define FLIGHT_DESTINATION_MAX_SIZE 20*sizeof(char)

#endif //_DEFINITIONS_H_
